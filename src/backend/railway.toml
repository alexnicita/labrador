[build]
builder = "RAILPACK"
buildCommand = "cargo build --release"

[deploy]
startCommand = "./target/release/labrador-realtime"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
